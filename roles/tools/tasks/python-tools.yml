---
- name: Ensure pipx is available
  ansible.builtin.apt:
    name: pipx
    state: present

- name: Install Python tools via pipx
  community.general.pipx:
    name: "{{ item.name }}"
    source: "{{ item.source }}"
    state: present
  loop: "{{ pipx_tools }}"
  become: false
