---
- name: Install Obsidian
  ansible.builtin.apt:
    name: obsidian
    state: present

- name: Deploy Obsidian launcher script
  ansible.builtin.copy:
    src: obsidian
    dest: /usr/local/bin/obsidian
    mode: "0755"
